model: yolov8n.pt
dataset: Dataset/ChemEq25_training/data.yaml
epochs: 100
batch_size: 16
image_size: 640
device: 'cpu'
seed: 42
deterministic: True
confidence_threshold: 0.25
project: outputs
name: baseline_training
workers: 0
